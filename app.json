{
  "name": "BookSeller",
  "build": {
    "publish": {
      "web": "java -Dspring.profiles.active=prod -Dserver.port=$PORT -jar build/libs/*.jar"
    }
  },
  "start": {
    "web": "java -Dspring.profiles.active=prod -Dserver.port=$PORT -jar build/libs/*.jar"
  }
}
